spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${jackie.blog.nacos.server.url}
      config:
        server-addr: ${jackie.blog.nacos.server.url}
        file-extension: properties
        name: ${spring.application.name}